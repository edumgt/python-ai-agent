# 성과 개선 및 리밸런싱

> **모듈 9: 데이터 활용 퀀트 모델링** | 2/5일차 | ⚙️ | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 포트폴리오 리밸런싱 성과 개선](https://www.youtube.com/results?search_query=포트폴리오+리밸런싱+성과+개선+파이썬+한국어)

## 오늘 배울 것 (아주 쉽게)

- 과최적화(Overfitting) 방지: 워크포워드 분석
- 거래비용(슬리피지, 수수료) 반영
- 리밸런싱 주기별 성과 비교
- 포지션 사이징(Kelly Criterion 등)
- 실습: 리밸런싱 전략 개선 실습

---



### 1. 과최적화(Overfitting) 방지: 워크포워드 분석

**쉽게 이해하기**
- **워크포워드(Walk-Forward)** 는 훈련 기간과 테스트 기간을 시간 순으로 분리해 순차적으로 검증하는 방법입니다.
- In-sample에서만 잘 맞는 파라미터가 Out-of-sample에서도 유효한지 반드시 확인해야 합니다.

**미니 예시**
```python
# 워크포워드: 훈련/테스트 기간을 순차적으로 분리
years = list(range(2015, 2025))
train_window = 3  # 3년 학습 윈도우

for i in range(train_window, len(years)):
    train = years[i - train_window: i]
    test  = years[i]
    print(f'학습: {train[0]}~{train[-1]} → 테스트: {test}')
# 출력 예시:
# 학습: 2015~2017 → 테스트: 2018
# 학습: 2016~2018 → 테스트: 2019
# 학습: 2017~2019 → 테스트: 2020
```

### 2. 거래비용(슬리피지, 수수료) 반영

**쉽게 이해하기**
- **수수료(Commission)** 는 거래당 고정 비용, **슬리피지(Slippage)** 는 호가 스프레드로 인한 불리한 체결 비용입니다.
- 거래비용을 반영하지 않으면 백테스트 수익률이 실제보다 과대 계산됩니다.

**미니 예시**
```python
# 거래비용 반영 — 수수료 + 슬리피지
commission = 0.001   # 0.1% 수수료
slippage   = 0.0005  # 0.05% 슬리피지
cost_per_trade = commission + slippage

returns = [0.01, -0.02, 0.015, 0.005]
trade   = [1, 0, 1, 1]   # 매매 발생 여부 (1=매매, 0=보유)
adj     = [r - cost_per_trade * t for r, t in zip(returns, trade)]

print('비용 전:', [f'{r:.3%}' for r in returns])
print('비용 후:', [f'{r:.3%}' for r in adj])
# 비용 후 수익률이 낮아지는 것을 확인
```

### 3. 리밸런싱 주기별 성과 비교

**쉽게 이해하기**
- 리밸런싱을 자주 할수록 목표 비중 유지에 유리하지만 거래비용이 누적됩니다.
- **월별 vs 분기별 vs 연간** 리밸런싱의 비용 차이를 먼저 계산하고, 실제 성과와 비교하세요.

**미니 예시**
```python
# 리밸런싱 주기별 연간 거래비용 비교
years = 3
options = {'월별': 12, '분기별': 4, '반기별': 2, '연간': 1}
cost_per_trade = 0.001  # 수수료 0.1%

for label, freq in options.items():
    annual_cost = freq * cost_per_trade
    total_cost  = annual_cost * years
    print(f'{label:5s} ({freq:2d}회/년): 연간비용 {annual_cost:.2%}, {years}년 누적 {total_cost:.2%}')
```

### 4. 포지션 사이징(Kelly Criterion 등)

**쉽게 이해하기**
- **켈리 공식** f\* = (b·p − q) / b 로 최적 베팅 비율을 계산합니다 (b=손익비, p=승률, q=패율).
- 실전에서는 변동성을 줄이기 위해 켈리 비율의 절반(반켈리)을 적용하는 것이 일반적입니다.

**미니 예시**
```python
# 켈리 공식으로 최적 포지션 비율 계산
win_rate = 0.55    # 승률 55%
avg_win  = 0.02    # 평균 수익 2%
avg_loss = 0.01    # 평균 손실 1%
b = avg_win / avg_loss  # 손익비 = 2.0

kelly = (b * win_rate - (1 - win_rate)) / b
half_kelly = kelly * 0.5  # 반켈리: 안전 마진 확보

print(f'손익비(b):       {b:.1f}')
print(f'풀켈리 비율:     {kelly:.2%}')
print(f'반켈리(권장):    {half_kelly:.2%}')
```

### 5. 실습: 리밸런싱 전략 개선 실습

**쉽게 이해하기**
- 거래비용, 워크포워드, 켈리 비율을 모두 적용한 뒤 성과 지표가 개선되는지 확인합니다.
- 개선 전후의 Sharpe, MDD, 승률을 비교 테이블로 정리하면 포트폴리오에 설득력이 생깁니다.

**미니 예시**
```python
# 개선 전후 성과 지표 비교 템플릿
before = {'CAGR': 0.082, 'Sharpe': 0.95, 'MDD': -0.18}
after  = {'CAGR': 0.079, 'Sharpe': 1.12, 'MDD': -0.12}

print(f"{'지표':10s} {'개선 전':>10s} {'개선 후':>10s} {'변화':>10s}")
print("-" * 44)
for key in before:
    b, a = before[key], after[key]
    diff = a - b
    sign = '+' if diff >= 0 else ''
    print(f"{key:10s} {b:>10.2%} {a:>10.2%} {sign}{diff:>9.2%}")
```

---

## 🌐 Lumina Invest 웹앱에서는 이렇게 써요!

> 오늘 배운 "더 똑똑한 백테스트" 기법들이 웹앱 어디에 녹아있는지 탐험해봐요!

---

### 🧪 워크포워드(Walk-Forward): 과거로 커닝하지 않기

웹앱의 LightGBM 모델은 데이터를 **앞 80%는 학습용, 뒤 20%는 검증용**으로 나눠요.  
이게 워크포워드의 가장 기본 형태예요 — 미래 데이터로 커닝하지 않는 거죠!

```python
# 📁 app/services/quant_pipeline.py  (122~132번째 줄)

X = df[FEATURE_COLS].values   # 전체 데이터
y = df["target"].values

split = int(len(X) * 0.8)    # ← 80% 앞부분 = 학습 기간
X_tr = X[:split]              # 과거 데이터로 학습 (In-sample)
X_va = X[split:]              # 최근 데이터로 검증 (Out-of-sample)

# LightGBM이 학습 후 검증 정확도를 계산해요
model = lgb.train(params, train_data, valid_sets=[val_data])
acc = (예측값 == 실제값).mean()   # "새로운 데이터에서 얼마나 맞혔나?"
```

> **초등생 비유:** 시험 공부할 때 교과서(과거 데이터)로 공부하고,  
> 실제 시험(미래 데이터)에서 점수를 받는 것과 똑같아요.  
> 교과서 문제만 외워서 시험을 보면 (과최적화) 실제 시험에서 망하죠!

---

### 💸 거래비용: 수수료도 빠짐없이 계산

웹앱의 `backtest_custom_indicator()` 함수는 현재 기본 백테스트를 제공하는데,  
**퀀트 설정 메뉴**에서 증권사를 선택하면 실제 수수료 기준이 달라져요.

```python
# 📁 app/routes/stocks.py  (249~265번째 줄)

class QuantSettingsBody(BaseModel):
    per_trade_budget: float = 1_000_000  # 거래당 예산 (1백만원)
    buy_ratio:        float = 1.0        # 매수할 때 예산의 몇 %를 쓸지
    sell_ratio:       float = 0.5        # 매도할 때 몇 %를 팔지
```

> **초등생 비유:** 편의점에서 물건 살 때 가격표가 있잖아요.  
> 주식도 살 때마다 0.015% 정도 수수료가 빠져요.  
> 하루에 100번 사고팔면 수수료가 엄청 쌓이니까, 자주 거래하면 손해가 날 수 있어요!

---

### ⏰ 리밸런싱 주기: 1시간마다 자동으로 새로고침

웹앱의 **데이터 동기화 스케줄러**가 리밸런싱 주기 개념을 그대로 쓰고 있어요!  
인터넷이 연결되면 1시간마다 KOSPI·나스닥·환율·거시경제 지표를 자동으로 새로 가져와요.

```python
# 📁 app/services/sync_scheduler.py  (최하단)

SYNC_INTERVAL_SEC = 3600   # ← 1시간(3600초)마다 = "매 시간 리밸런싱"

async def _scheduler_loop():
    await asyncio.sleep(5)    # 앱 시작 5초 후 첫 실행
    while True:
        await sync_all()      # 모든 데이터 새로고침
        await asyncio.sleep(SYNC_INTERVAL_SEC)   # 1시간 대기 후 반복
```

| 리밸런싱 주기 | 웹앱 적용 | 거래 횟수/년 |
|---|---|---|
| **매 시간** (현재) | 데이터 동기화 | 8,760회 |
| 월별 | 일반 포트폴리오 | 12회 |
| 분기별 | 장기 투자자 | 4회 |

> **초등생 비유:** 냉장고 재고를 확인하는 것과 같아요.  
> 매일 확인하면 항상 신선하지만 수고가 많이 들고,  
> 한 달에 한 번만 확인하면 편하지만 음식이 다 떨어질 수 있어요!

---

### 🎯 켈리 공식: "얼마나 살까?" 를 계산하는 웹앱 설정

켈리 공식은 최적 투자 비율을 알려줘요. 웹앱의 퀀트 자동매매 설정이 바로 이 개념을 쓰고 있어요!

```python
# 📁 app/routes/stocks.py  (QuantSettingsBody 클래스)

per_trade_budget: float = 1_000_000   # 한 번 거래에 쓸 최대 금액
buy_ratio:        float = 1.0         # 켈리 비율 = 예산의 100%  ← 풀켈리
# buy_ratio = 0.5 로 바꾸면               예산의 50%            ← 반켈리 (권장!)
```

```python
# 켈리 공식으로 웹앱 설정값 이해하기
win_rate = 0.55   # 퀀트 모델 승률 55%
avg_win  = 0.02   # 평균 수익 2%
avg_loss = 0.01   # 평균 손실 1%
b = avg_win / avg_loss  # 손익비 = 2

kelly      = (b * win_rate - (1 - win_rate)) / b  # = 32.5%
half_kelly = kelly * 0.5                           # = 16.25% ← buy_ratio 권장값

print(f"웹앱 buy_ratio 권장값: {half_kelly:.1%}")
# 웹앱에서 per_trade_budget * half_kelly = 실제 매수 금액
```

> **초등생 비유:** 제비뽑기에서 이길 확률이 55%라면,  
> "전 재산을 다 거는 것"(풀켈리) 보다  
> "전 재산의 절반만 거는 것"(반켈리)이 훨씬 안전해요!

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 058 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


