# 백테스트 성과 지표 분석

> **모듈 9: 데이터 활용 퀀트 모델링** | 1/5일차 | ⚙️ | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 백테스트 성과 지표 샤프비율 MDD](https://www.youtube.com/results?search_query=백테스트+성과지표+샤프비율+MDD+파이썬+한국어)

## 오늘 배울 것 (아주 쉽게)

- 백테스트(Backtest) 개념과 프레임워크
- MDD(Maximum Drawdown) 계산 및 해석
- 샤프 비율(Sharpe Ratio) 계산
- 칼마 비율(Calmar Ratio), 승률(Win Rate)
- 실습: vectorbt / backtrader로 백테스트 실행

---



### 1. 백테스트(Backtest) 개념과 프레임워크

**쉽게 이해하기**
- **백테스트(Backtest) 개념과 프레임워크** 개념은 `일별 수익률 계산 → 누적 성과 확인 → 리스크 지표 비교`로 이해하면 쉽습니다.
- 먼저 작은 구간에서 계산이 맞는지 검증한 뒤, 전체 기간으로 확장하세요.

**미니 예시**
```python
# 백테스트(Backtest) 개념과 프레임워크 미니 예시
returns = [0.01, -0.02, 0.015, 0.005]
cum = 1.0
for r in returns:
    cum *= (1 + r)
print(f'누적수익률: {(cum - 1):.2%}')
```

### 2. MDD(Maximum Drawdown) 계산 및 해석

**쉽게 이해하기**
- **MDD(Maximum Drawdown)** 는 고점 대비 최대 낙폭으로, 전략이 경험한 최악의 손실 크기를 나타냅니다.
- -15% 이내를 합격선으로 보며, 값이 클수록 심리적·자금 리스크가 커집니다.

**미니 예시**
```python
# 고점 대비 최대 낙폭(MDD) 계산
prices = [100, 105, 103, 110, 95, 98, 102]
peak = prices[0]
mdd = 0.0
for p in prices:
    if p > peak:
        peak = p
    drawdown = (p - peak) / peak
    mdd = min(mdd, drawdown)
print(f'MDD: {mdd:.2%}')  # MDD: -13.64%
```

### 3. 샤프 비율(Sharpe Ratio) 계산

**쉽게 이해하기**
- **샤프 비율(Sharpe Ratio)** = (연평균수익 − 무위험수익) / 연환산변동성으로, 1.0 이상이면 합격선입니다.
- 수익률이 같아도 변동성이 크면 샤프 비율이 낮으므로 안정성 비교에 적합합니다.

**미니 예시**
```python
# 연환산 샤프 비율 계산 (무위험수익률 연 3% 가정)
import math
returns = [0.01, -0.02, 0.015, 0.005, -0.01, 0.02]
rf_daily = 0.03 / 252
excess = [r - rf_daily for r in returns]
mean_e = sum(excess) / len(excess)
var_e = sum((e - mean_e) ** 2 for e in excess) / len(excess)
sharpe = (mean_e / math.sqrt(var_e)) * math.sqrt(252)
print(f'샤프 비율: {sharpe:.2f}')  # 1.0 이상이면 합격
```

### 4. 칼마 비율(Calmar Ratio), 승률(Win Rate)

**쉽게 이해하기**
- **승률** = 수익 거래 수 / 전체 거래 수, **칼마 비율** = 연평균수익률 / |MDD| 로 전략 안정성을 확인합니다.
- 승률이 낮아도 손익비가 높으면 수익이 날 수 있으므로 두 지표를 함께 봐야 합니다.

**미니 예시**
```python
# 승률과 칼마 비율 계산
returns = [0.01, -0.02, 0.015, 0.005, -0.01, 0.02]
win_rate = sum(1 for r in returns if r > 0) / len(returns)
print(f'승률: {win_rate:.2%}')  # 승률: 66.67%

# 칼마 비율 = 연평균수익률 / |MDD|
annual_ret = sum(returns) / len(returns) * 252
prices = [1.0]
for r in returns:
    prices.append(prices[-1] * (1 + r))
peak = prices[0]
mdd = 0.0
for p in prices:
    if p > peak:
        peak = p
    mdd = min(mdd, (p - peak) / peak)
calmar = annual_ret / abs(mdd)
print(f'칼마 비율: {calmar:.2f}')
```

### 5. 실습: vectorbt / backtrader로 백테스트 실행

**쉽게 이해하기**
- **vectorbt**는 pandas 확장으로 빠른 벡터 연산 백테스트, **backtrader**는 이벤트 기반으로 복잡한 전략 구현에 적합합니다.
- 라이브러리 없이도 `position = signal.shift(1)` 패턴으로 직접 구현할 수 있습니다.

**미니 예시**
```python
# vectorbt 없이 직접 구현하는 MA 크로스오버 백테스트
import numpy as np

# 가상 일별 수익률과 포지션 신호
returns = [0.01, -0.02, 0.015, -0.005, 0.02, 0.008, -0.01, 0.012]
signal  = [1,    1,     0,      0,      1,    1,      1,     1   ]  # 1=보유, 0=현금

# 전략 수익률 = 수익률 × 전날 포지션 (미래 데이터 방지)
strat = [r * s for r, s in zip(returns[1:], signal[:-1])]

cum = 1.0
for r in strat:
    cum *= (1 + r)
sharpe = np.mean(strat) / np.std(strat) * np.sqrt(252)
print(f'전략 누적수익률: {(cum - 1):.2%}')
print(f'샤프 비율:       {sharpe:.2f}')

# vectorbt 사용 시 (pip install vectorbt)
# import vectorbt as vbt
# pf = vbt.Portfolio.from_signals(price, entries, exits, freq='1D')
# print(pf.stats())
```

---

## 🌐 Lumina Invest 웹앱에서는 이렇게 써요!

> 방금 배운 지표들이 실제 웹앱 코드 어디에 살아있는지 같이 찾아봐요.  
> 마치 **요리 레시피**를 배웠는데, 실제 식당 주방에서 그 레시피가 어떻게 쓰이는지 구경하는 것과 같아요!

---

### 🏠 백테스트가 사는 집: `app/services/quant_pipeline.py`

웹앱의 **퀀트 메뉴 → 퀀트 대시보드**를 누르면, 브라우저가 서버에 이렇게 물어봐요:

> "삼성전자 주가를 2년치 가져다가, ML 모델로 매수/매도 신호를 만들고, 백테스트 결과를 알려줘!"

그 요청을 처리하는 함수가 바로 `backtest()` 함수예요. 이 함수가 하는 일을 아주 쉽게 풀어볼게요:

```python
# 📁 app/services/quant_pipeline.py  (실제 코드 요약)

def backtest(df, signals):
    # 1️⃣  오늘 수익률 계산
    ret = df["close"].pct_change()

    # 2️⃣  "어제 매수 신호가 있었으면 오늘 보유"  ← 미래 데이터 커닝 방지!
    pos = signals.shift(1)
    strat_ret = ret * (pos == 1)          # 전략 수익률

    # 3️⃣  샤프 비율: "위험 대비 얼마나 벌었나?"
    sharpe = strat_ret.mean() / strat_ret.std() * √252

    # 4️⃣  MDD: "가장 힘들었던 순간의 손실"
    drawdown = (누적수익 - 누적수익_최고점) / 누적수익_최고점
    mdd = drawdown.min()   # 가장 낮은 값 = 최대 낙폭

    # 5️⃣  승률: "이긴 날 / 전체 매매 날"
    win_rate = (strat_ret > 0).sum() / len(strat_ret)

    return { "sharpe_ratio": ..., "mdd_pct": ..., "win_rate_pct": ... }
```

> **초등생 비유:** 백테스트는 마치 "타임머신"이에요.  
> 과거로 돌아가서 "내가 이 전략대로 투자했으면 얼마 벌었을까?" 를 컴퓨터로 시뮬레이션 하는 거예요.  
> 실제 돈을 쓰지 않고 연습해볼 수 있어서 아주 안전하죠!

---

### 📊 웹앱 화면에서 보이는 지표들

**퀀트 대시보드** 화면에 뜨는 숫자들이 바로 `backtest()` 함수가 돌려준 값이에요:

| 화면에 보이는 것 | 코드 변수 이름 | 쉬운 설명 |
|---|---|---|
| 총 수익률 | `total_return_pct` | "이 전략으로 얼마 벌었나?" |
| 샤프 비율 | `sharpe_ratio` | 1.0 넘으면 우수한 전략 |
| MDD | `mdd_pct` | -15% 이내면 합격 |
| 승률 | `win_rate_pct` | 이긴 날 비율 |
| 매매 횟수 | `trade_count` | 몇 번 사고 팔았나 |

---

### 🔍 MDD 계산: 웹앱 코드로 직접 확인

```python
# 📁 app/services/quant_pipeline.py  (242~266번째 줄)

roll_max = cum_strat.cummax()          # 지금까지 가장 높았던 자산 금액
drawdown = (cum_strat - roll_max) / roll_max   # 고점에서 얼마나 빠졌나
mdd      = float(drawdown.min()) * 100          # 가장 많이 빠진 순간 (%)
```

> **초등생 비유:** 롤러코스터를 생각해봐요.  
> 가장 높이 올라갔다가 `(roll_max)` → 가장 많이 내려간 곳까지의 거리가 MDD예요.  
> -15% MDD면 "100만원이 85만원까지 내려갔다"는 뜻이에요.

---

### 📡 API 경로: 백테스트 결과를 화면으로 보내는 통로

```
브라우저 → GET /api/quant/pipeline?symbol=005930.KS
         → app/routes/stocks.py (626번째 줄)
         → quant_pipeline.run_pipeline(candles)
         → backtest() 실행
         → { sharpe_ratio: 1.2, mdd_pct: -8.5, win_rate_pct: 58.3 }
         → 화면에 카드로 표시
```

> **초등생 비유:** 브라우저(사용자)가 서버(주방)에 주문하면,  
> 주방에서 요리(계산)해서 완성된 음식(숫자)을 화면(테이블)에 가져다 주는 거예요!

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 057 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```

