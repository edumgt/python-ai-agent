# PineScript 실습 및 전략 검증

> **모듈 9: 데이터 활용 퀀트 모델링** | 5/5일차 | ⚙️ | 학습시간: 8시간



---

> 📺 **YouTube 강의**: [🎬 PineScript 실습 전략 검증 TradingView](https://www.youtube.com/results?search_query=PineScript+TradingView+전략+백테스트+한국어)

## 오늘 배울 것 (아주 쉽게)

- PineScript로 이동평균 전략 구현
- PineScript 백테스트 결과 해석
- Python 전략과 PineScript 결과 비교
- 실습: 나만의 간단한 트레이딩 전략 PineScript 구현

---



### 1. PineScript로 이동평균 전략 구현

**쉽게 이해하기**
- `ta.crossover(fast, slow)` 한 줄로 골든크로스를, `ta.crossunder(fast, slow)` 로 데드크로스를 감지할 수 있습니다.
- `label.new()` 로 신호 발생 시점에 차트에 화살표를 표시하면 시각적 검증이 쉬워집니다.

**미니 예시**
```pine
// PineScript v5 — 이동평균 골든크로스/데드크로스 전략
//@version=5
strategy("MA 골든크로스", overlay=true)

ma20 = ta.sma(close, 20)
ma60 = ta.sma(close, 60)

// 신호 조건
long_cond  = ta.crossover(ma20, ma60)   // 골든크로스 → 매수
short_cond = ta.crossunder(ma20, ma60)  // 데드크로스 → 청산

// 매매 실행
if long_cond
    strategy.entry("매수", strategy.long)
    label.new(bar_index, low,  "▲", style=label.style_label_up,   color=color.green)

if short_cond
    strategy.close("매수")
    label.new(bar_index, high, "▽", style=label.style_label_down, color=color.red)

// 이동평균 시각화
plot(ma20, color=color.blue,   linewidth=2, title="MA20")
plot(ma60, color=color.orange, linewidth=2, title="MA60")
```

### 2. PineScript 백테스트 결과 해석

**쉽게 이해하기**
- TradingView **전략 테스터** 탭의 Net Profit, Max Drawdown, Win Rate, Profit Factor, Sharpe Ratio 5가지를 기준값과 비교합니다.
- Profit Factor > 1.5, MDD > -15%, Sharpe > 1.0 을 동시에 만족해야 포트폴리오 제출 기준에 부합합니다.

**미니 예시**
```python
# TradingView 전략 테스터 결과 합격 기준 자동 체크
backtest = {
    'Net Profit (%)':   15.24,
    'Max Drawdown (%)': -8.3,
    'Win Rate (%)':     56.5,
    'Profit Factor':     1.8,
    'Sharpe Ratio':      1.12,
}
criteria = {
    'Net Profit (%)':   ('>0',    lambda v: v > 0),
    'Max Drawdown (%)': ('>-15%', lambda v: v > -15),
    'Win Rate (%)':     ('>50%',  lambda v: v > 50),
    'Profit Factor':    ('>1.5',  lambda v: v > 1.5),
    'Sharpe Ratio':     ('>1.0',  lambda v: v > 1.0),
}
print(f"{'지표':22s} {'값':>8s} {'기준':>8s}  결과")
print("-" * 52)
for key, val in backtest.items():
    crit_str, crit_fn = criteria[key]
    status = '✅ 합격' if crit_fn(val) else '⚠️ 미달'
    print(f"{key:22s} {val:>8.2f} {crit_str:>8s}  {status}")
```

### 3. Python 전략과 PineScript 결과 비교

**쉽게 이해하기**
- Python과 PineScript 결과가 완전히 일치하지 않을 수 있습니다. 데이터 소스·거래비용·체결 시점 가정이 다르기 때문입니다.
- 두 결과의 방향성(Sharpe 증감, MDD 변화)이 일치하면 전략이 일관성 있다고 볼 수 있습니다.

**미니 예시**
```python
# Python 백테스트 vs PineScript 전략 테스터 결과 비교
python_result = {
    'CAGR (%)':    8.5,
    'Sharpe':      1.08,
    'MDD (%)':    -9.2,
    'Win Rate (%)': 54.3,
}
pine_result = {
    'CAGR (%)':    8.3,    # 약간 낮음: 데이터·수수료 차이
    'Sharpe':      1.05,
    'MDD (%)':    -9.5,
    'Win Rate (%)': 53.8,
}
print(f"{'지표':15s} {'Python':>10s} {'PineScript':>12s} {'차이':>8s}")
print("-" * 48)
for key in python_result:
    py = python_result[key]
    ps = pine_result[key]
    print(f"{key:15s} {py:>10.2f} {ps:>12.2f} {py-ps:>+8.2f}")
```

### 4. 실습: 나만의 간단한 트레이딩 전략 PineScript 구현

**쉽게 이해하기**
- `ta.rsi()`, `ta.macd()`, `ta.bollinger_bands()` 등 내장 지표를 조합해 진입·청산 조건을 만들어보세요.
- 전략 테스터 결과에서 Profit Factor와 MDD를 우선 확인하고, 파라미터를 조정해 개선 방향을 찾습니다.

**미니 예시**
```pine
// PineScript v5 — RSI 역추세 전략 (과매도 매수 / 과매수 청산)
//@version=5
strategy("RSI 역추세", overlay=false, initial_capital=10000,
         commission_type=strategy.commission.percent, commission_value=0.1)

// 파라미터
rsi_len    = input.int(14, "RSI 기간",    minval=5,  maxval=30)
oversold   = input.int(30, "과매도 기준", minval=10, maxval=40)
overbought = input.int(70, "과매수 기준", minval=60, maxval=90)

rsi = ta.rsi(close, rsi_len)

// 시각화
plot(rsi, color=color.purple, title="RSI", linewidth=2)
hline(oversold,   "과매도", color=color.green, linestyle=hline.style_dashed)
hline(overbought, "과매수", color=color.red,   linestyle=hline.style_dashed)
hline(50,         "중립",   color=color.gray,  linestyle=hline.style_dotted)

// 매매 조건
if rsi < oversold
    strategy.entry("매수", strategy.long)   // 과매도 → 매수

if rsi > overbought
    strategy.close("매수")                  // 과매수 → 청산
```

---

## 🌐 Lumina Invest 웹앱에서는 이렇게 써요!

> PineScript로 TradingView에서 하는 일을 Python으로 웹앱 안에서 똑같이 할 수 있어요.  
> "TradingView 전략 테스터 = 웹앱 퀀트 파이프라인" 이라고 생각하면 돼요!

---

### 🔀 PineScript ↔ 웹앱 Python 1:1 대응표

| TradingView PineScript | Lumina Invest 웹앱 Python |
|---|---|
| `ta.sma(close, 20)` | `close.rolling(20).mean()` |
| `ta.rsi(close, 14)` | `quant_pipeline.py`의 RSI 계산 |
| `ta.crossover(ma20, ma60)` | `golden_cross = (ma_short > ma_mid) & (ma_short.shift(1) <= ma_mid.shift(1))` |
| `strategy.entry("매수", ...)` | `await broker.place_order(..., "buy", ...)` |
| 전략 테스터 결과 (Sharpe/MDD) | `backtest()` 함수 반환값 |
| Pine Editor | 웹앱 퀀트 메뉴 → 인디케이터 백테스트 |

---

### 1️⃣ MA 골든크로스: PineScript → Python 완전 번역

```pine
// 📺 TradingView PineScript (화면에서 보이는 코드)
ma20 = ta.sma(close, 20)
ma60 = ta.sma(close, 60)
long_cond = ta.crossover(ma20, ma60)   // 골든크로스
if long_cond
    strategy.entry("매수", strategy.long)
```

```python
# 📁 app/services/quant_pipeline.py → backtest_custom_indicator()
# 위 PineScript를 Python으로 번역한 것이에요!

ma_short = close.rolling(short_window).mean()   # ta.sma(close, 20)
ma_mid   = close.rolling(mid_window).mean()     # ta.sma(close, 60)

# ta.crossover(ma20, ma60) 번역
golden_cross = (ma_short > ma_mid) & (ma_short.shift(1) <= ma_mid.shift(1))
dead_cross   = (ma_short < ma_mid) & (ma_short.shift(1) >= ma_mid.shift(1))

# RSI 조건까지 추가해서 더 정교하게
buy_cond  = golden_cross & (rsi < buy_threshold)   # 골든크로스 + RSI 과매도
sell_cond = dead_cross   | (rsi > sell_threshold)  # 데드크로스 또는 RSI 과매수

# strategy.entry() 번역 → 포지션 시리즈 생성
position = regime.ffill().fillna(0.0)   # 1.0=보유, 0.0=현금
```

> **초등생 비유:** PineScript는 **영어 레시피**, Python은 **한국어 레시피**예요.  
> 만드는 음식(전략)은 똑같아요. 단지 언어만 다를 뿐이에요!

---

### 2️⃣ 전략 테스터 결과 해석: 웹앱이 계산해주는 5가지 성적표

TradingView 전략 테스터가 보여주는 수치를 웹앱의 `backtest()` 함수가 동일하게 계산해요:

```python
# 📁 app/services/quant_pipeline.py  (241~290번째 줄)
# → GET /api/quant/pipeline 을 호출하면 아래 성적표를 돌려줘요

def backtest(df, signals):
    return {
        # ✅ Net Profit      → "total_return_pct"   (기준: > 0%)
        "total_return_pct":    15.24,

        # ✅ Sharpe Ratio    → "sharpe_ratio"        (기준: > 1.0)
        "sharpe_ratio":         1.12,

        # ✅ Max Drawdown    → "mdd_pct"             (기준: > -15%)
        "mdd_pct":             -8.3,

        # ✅ Win Rate        → "win_rate_pct"        (기준: > 50%)
        "win_rate_pct":        56.5,

        # ✅ Trade Count     → "trade_count"         (거래 횟수)
        "trade_count":          42,
    }
```

> **초등생 비유:** 학교 성적표처럼 5가지 과목 점수가 있어요.  
> 모든 과목이 합격선을 넘어야 "이 전략은 실전에서 쓸 수 있어요!"라고 할 수 있어요.

---

### 3️⃣ 웹앱에서 백테스트 직접 실행해보기

**퀀트 메뉴 → 인디케이터 백테스트** 화면에서 파라미터를 조정하면 서버가 실시간으로 계산해줘요:

```
사용자 입력                API 경로                       백테스트 실행
────────────              ─────────                      ──────────────
단기 MA: 5일       →     GET /api/quant/pipeline      →  backtest_custom_indicator(
중기 MA: 20일             ?symbol=005930.KS                short_window=5,
RSI 기간: 14일            &short=5&mid=20&rsi=14           mid_window=20,
매수 기준: RSI<35         &buy_th=35                       rsi_period=14,
                                                            buy_threshold=35.0
                                                         )
                          ← { sharpe: 1.2, mdd: -8.5 }  ←  결과 반환
```

이게 바로 PineScript의 **"파라미터 슬라이더"** 를 Python + FastAPI로 만든 거예요!

---

### 4️⃣ Python vs PineScript: 두 결과가 약간 다른 이유

```python
# 📁 app/services/quant_pipeline.py  (257번째 줄)

# 웹앱 (Python) — 수수료 없이 단순 계산
strat_ret = ret * (pos == 1).astype(float)

# TradingView (PineScript) — 수수료 0.1% 포함
# commission_type=strategy.commission.percent, commission_value=0.1
```

| 항목 | 웹앱 Python | TradingView PineScript |
|---|---|---|
| 데이터 소스 | Yahoo Finance | TradingView 자체 |
| 수수료 반영 | 현재 미포함 | 0.1% 설정 가능 |
| 슬리피지 | 미포함 | 설정 가능 |
| 체결 시점 | shift(1) 전날 신호 | 다음 봉 시가 |

> **초등생 비유:** 같은 요리 레시피라도 요리사마다 소금을 조금 다르게 넣으면  
> 맛이 약간 달라지잖아요? Python과 PineScript 결과도 이런 작은 차이가 있어요.  
> 중요한 건 **"Sharpe가 모두 1 이상이냐"** 같은 방향성이 일치하는지예요!

---

### 🎁 웹앱 전체 퀀트 파이프라인 한눈에 보기

```
[Yahoo Finance API]        [웹앱 백엔드]               [웹앱 화면]
   주가 OHLCV        →    preprocess()           →    📊 퀀트 차트
                     →    feature_engineer()     →    📈 기술 지표
                     →    train_lgb() / mlp()    →    🤖 ML 신호
                     →    rule_based_signals()   →    🎯 매매 시그널
                     →    backtest()             →    📋 성적표 카드
                     →    alpaca_execute()       →    🛒 자동 주문

                    PineScript 전략 테스터와 동일한 역할!
```

**웹앱 퀀트 메뉴에서 이 모든 과정이 버튼 하나로 실행돼요:**
- 퀀트 대시보드 → 종목 선택 → 모델 선택 (LightGBM/MLP/규칙 기반) → 결과 확인

---

## 해보기 활동

오늘 배운 것을 이용해 아래 활동을 해봐요.

```python
# Day 061 실습 과제
# 1) 샘플 데이터를 준비하고
# 2) 오늘 배운 개념(지표/모델/분석 중 1개 이상)을 적용한 뒤
# 3) 결과를 한 줄 요약으로 출력해보세요.

# 예시 데이터: 일별 종가(가상의 가격 시계열)
data = [100, 103, 101, 106, 108]

# TODO: 아래에 학습 내용을 반영한 코드를 작성하세요.
result = None

print("분석 결과:", result)
```


